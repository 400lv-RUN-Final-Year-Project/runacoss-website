## Payment Endpoints (Expected by Frontend)

- `POST /api/payments/verify` — Verifies a payment after Paystack callback.
- `POST /api/payments/send-receipt` — Sends a payment receipt to the user via email. 